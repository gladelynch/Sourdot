package install

// Collision-proof versions-directory layout: every install lives in its own
// subdirectory named by its full synthetic InstalledVersion.ID (see
// platform.VersionsDir for the parent directory). Implemented in M1.
